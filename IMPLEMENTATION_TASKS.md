# مهام تبسيط وتنفيذ رتّبها AI

هذا الملف هو مرجع التنفيذ. ننجز مهمة واحدة في كل مرة، ونحدّث الحالة بعد كل خطوة.

## قواعد العمل

- كل مهمة تنتهي ببناء ناجح واختبارات ناجحة: `pnpm build` و `pnpm test`.
- أي تغيير في تجربة المستخدم يكون عربي وRTL أولاً.
- لا نضيف صفحات جديدة إلا لو كانت ضرورية جداً.
- الأولوية دائماً: منفعة حقيقية للمستخدم قبل اكتمال الشكل.
- لا نعتمد على `MOCK_PLAN` في واجهات المستخدم الأساسية بعد مرحلة التوحيد.

## المرحلة 0: ترتيب الأساس

- [x] `RAI-000` إنشاء ملف مهام التنفيذ.
  - الناتج: ملف واحد يحدد الأولويات وخطوات العمل.

- [x] `RAI-001` تنظيف إعدادات Git المحلية من أي token داخل remote URL.
  - الناتج: `origin` يستخدم رابط GitHub آمن بدون أسرار.
  - معيار القبول: `git remote -v` لا يحتوي على token.

- [x] `RAI-002` توثيق نقطة البداية الحالية.
  - الناتج: سطر في هذا الملف يثبت آخر build/test ناجحين.
  - معيار القبول: تسجيل نتيجة `pnpm build` و `pnpm test` بتاريخ التنفيذ.

## المرحلة 1: مصدر بيانات واحد

- [x] `RAI-010` جعل الصفحة الرئيسية تقرأ من Zustand بدلاً من `MOCK_PLAN`.
  - الملفات المتوقعة: `src/app/page.tsx`, `src/lib/store/tasks-store.ts`.
  - معيار القبول: أي إنجاز أو تأجيل من صفحة اليوم يظهر في الرئيسية بعد الرجوع لها.

- [x] `RAI-011` جعل التقويم يقرأ من Zustand بدلاً من `MOCK_PLAN`.
  - الملفات المتوقعة: `src/app/calendar/page.tsx`.
  - معيار القبول: حذف أو تأجيل مهمة ينعكس في التقويم.

- [x] `RAI-012` نقل `PillStatus` من مكون الواجهة إلى `src/lib/types.ts`.
  - الملفات المتوقعة: `src/lib/types.ts`, `src/components/plan/StatusPill.tsx`, `src/lib/store/tasks-store.ts`.
  - معيار القبول: `lib/store` لا يستورد أي type من `components`.

- [x] `RAI-013` إضافة persist للـ Zustand store.
  - الملفات المتوقعة: `src/lib/store/tasks-store.ts`.
  - معيار القبول: المهام المنجزة والمؤجلة لا تختفي بعد refresh.

## المرحلة 2: تبسيط التجربة

- [x] `RAI-020` اعتبار `/today` الشاشة المركزية للتطبيق.
  - الناتج: صفحة اليوم تجمع المهمة التالية، قائمة اليوم، وإدخال سراج.
  - معيار القبول: المستخدم يستطيع إضافة كلام لسراج من نفس صفحة اليوم.

- [x] `RAI-021` تقليل BottomNav إلى 3 نقاط أساسية.
  - المقترح: `اليوم`، زر وسط `رتّب`، `الإعدادات`.
  - الملفات المتوقعة: `src/components/shell/BottomNav.tsx`.
  - معيار القبول: لا توجد تبويبات دائمة للشات أو التقويم في الـ MVP.

- [x] `RAI-022` تحويل preview/review إلى bottom sheet داخل التدفق بدلاً من صفحات منفصلة.
  - الملفات المتوقعة: مكونات جديدة أو إعادة استخدام `Sheet`.
  - معيار القبول: اعتماد الخطة أو تعديلها يتم من نفس الرحلة بدون انتقالات كثيرة.

- [x] `RAI-023` إخفاء أو تقليل صفحات MVP غير الأساسية من التنقل.
  - الصفحات المرشحة: `/calendar`, `/inbox`, `/plan/preview`, `/plan/review`.
  - معيار القبول: الصفحات تبقى موجودة لو احتجناها، لكنها لا تشتت المستخدم في المسار الأساسي.

## المرحلة 3: جعل سراج مفيداً فعلاً

- [x] `RAI-030` إنشاء pipeline محلي لبناء خطة من النص.
  - الملف المقترح: `src/lib/plan/build-plan.ts`.
  - المدخل: نص المستخدم.
  - المخرج: draft plan يحتوي buckets و inbox.
  - معيار القبول: لا يحتاج التدفق إلى `setTimeout` أو plan mock ثابت.

- [x] `RAI-031` ربط `/api/ai/parse-tasks` بالـ pipeline المحلي مؤقتاً.
  - الملفات المتوقعة: `src/app/api/ai/parse-tasks/route.ts`.
  - معيار القبول: API يرجع نتيجة مرتبطة بنص المستخدم ولو بذكاء محلي بسيط.

- [x] `RAI-032` استخدام `resolveArabicDate` داخل بناء الخطة.
  - الملفات المتوقعة: `src/lib/date/arabic-date-resolver.ts`, `src/lib/plan/build-plan.ts`.
  - معيار القبول: عبارات مثل "اليوم"، "بكرة"، "الخميس" تتحول لتواريخ فعلية.

- [x] `RAI-033` استخدام `scheduleTasks` داخل بناء الخطة.
  - الملفات المتوقعة: `src/lib/schedule/scheduling-engine.ts`, `src/lib/plan/build-plan.ts`.
  - معيار القبول: المهام لا تتداخل مع الأحداث الثابتة.

- [x] `RAI-034` عرض الأسئلة الغامضة داخل نفس التدفق.
  - معيار القبول: مهمة مثل "راجع الفواتير لما أفضى" تظهر كسؤال بسيط، لا كصفحة Inbox منفصلة.

## المرحلة 4: تقوية الحالة والاختبارات

- [x] `RAI-040` إضافة اختبارات للـ store actions.
  - الملفات المقترحة: `src/lib/store/tasks-store.test.ts`.
  - معيار القبول: اختبارات لـ `markDone`, `postponeItem`, `removeItem`, `reset`.

- [x] `RAI-041` إضافة اختبارات لبناء الخطة.
  - الملفات المقترحة: `src/lib/plan/build-plan.test.ts`.
  - معيار القبول: نص عربي واحد ينتج مهام مجدولة ومهام تحتاج توضيح.

- [x] `RAI-042` إزالة الاعتماد المباشر على mock data من واجهات المستخدم الأساسية.
  - معيار القبول: `MOCK_PLAN` يستخدم كبذرة للـ store أو tests فقط.

## المرحلة 5: تحسينات المنتج

- [x] `RAI-050` تحسين empty states في صفحة اليوم.
  - معيار القبول: لو اليوم فاضي، سراج يعطي دعوة واضحة لإدخال المهام.

- [x] `RAI-051` تحسين أفعال المهمة التالية.
  - الأفعال: `تم`، `أجّل`، `رتّب اليوم`.
  - معيار القبول: كل فعل يغير الحالة فعلاً ويظهر أثره فوراً.

- [x] `RAI-052` تقليل النصوص التطمينية المتكررة.
  - معيار القبول: رسالة الخصوصية تظهر في موضع واحد واضح بدل تكرارها في كل شاشة.

- [x] `RAI-053` مراجعة كل النصوص العربية الظاهرة.
  - معيار القبول: لا توجد نصوص إنجليزية أو عبارات demo في واجهة المستخدم.

## المرحلة 6: تكاملات لاحقة

- [x] `RAI-060` استبدال parser المحلي بتكامل Gemini فعلي.
  - معيار القبول: نفس schema الحالي يستقبل مخرجات Gemini بعد validation.

- [x] `RAI-061` تفعيل Google Calendar read.
  - معيار القبول: الأحداث الموجودة تقرأ كـ busy blocks للجدولة.

- [x] `RAI-062` تفعيل Google Calendar save.
  - معيار القبول: لا يتم الحفظ إلا بعد ضغط المستخدم على زر اعتماد واضح.

- [ ] `RAI-063` تفعيل Supabase بعد ثبات تجربة MVP.
  - معيار القبول: المستخدم يحتفظ بخططه عبر الأجهزة بعد تسجيل الدخول.
  - الحالة: تم تحويل `getSupabaseClient` إلى REST client آمن عند توفر مفاتيح Supabase، لكن تفعيل الحفظ عبر الأجهزة يحتاج مشروع Supabase ومفاتيح Auth وسياسات RLS مطبقة.

## سجل التحقق

- 2026-05-16: `pnpm typecheck` نجح.
- 2026-05-16: `pnpm lint` نجح بدون warnings.
- 2026-05-16: `pnpm test` نجح، 10 اختبارات من 10.
- 2026-05-16: `pnpm build` نجح.
- 2026-05-16: بعد `RAI-010`، `pnpm test` نجح 10/10 و `pnpm build` نجح.
- 2026-05-16: بعد `RAI-011` و `RAI-012` و `RAI-013`، `pnpm test` نجح 10/10 و `pnpm build` نجح.
- 2026-05-16: بعد `RAI-020` و `RAI-021` و `RAI-023`، `pnpm test` نجح 10/10 و `pnpm build` نجح.
- 2026-05-16: بعد `RAI-022`، `pnpm test` نجح 10/10 و `pnpm build` نجح.
- 2026-05-16: بعد `RAI-030` إلى `RAI-034`، `pnpm test` نجح 10/10 و `pnpm build` نجح.
- 2026-05-16: بعد `RAI-040` و `RAI-041` و `RAI-042`، `pnpm test` نجح 16/16 و `pnpm build` نجح.
- 2026-05-16: بعد `RAI-050` إلى `RAI-053`، `pnpm test` نجح 16/16 و `pnpm build` نجح.
- 2026-05-16: بعد `RAI-060` و `RAI-061` و `RAI-062` وتمهيد Supabase، `pnpm test` نجح 16/16 و `pnpm build` نجح.
- 2026-05-16: فحص متصفح محلي على `/today` و `/chat` نجح بعد إصلاح تحذير React ومشكلة hydration الخاصة بزر الصوت.
