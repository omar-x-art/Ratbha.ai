# خطة المرحلة القادمة

هذا الملف يجهز العمل القادم على رتّبها AI بحيث أقدر أنا أو أي مطور خارجي نكمل بدون إعادة اكتشاف القرار من الصفر.

## الهدف

نحوّل التطبيق من تجربة ترتيب مهام بسيطة إلى مساعد يومي أخف وأوضح، ثم نضيف طبقة متابعة وتذكير وإحصائيات تساعد المستخدم يفهم أين يذهب وقته، وما الذي يتحسن، وما الذي يحتاج تعديل.

## مبادئ التنفيذ

- الشاشة الأساسية تبقى بسيطة: اليوم، التقويم، رتّب، الإعدادات.
- كل مهمة لها وقت بداية ونهاية واضحان، وحالة متابعة يمكن قياسها.
- الإحصائيات لا تكون زخرفة؛ لازم تقترح سلوكاً مفيداً أو تكشف نمطاً واضحاً.
- لا نكرر نفس الإجراء أو نفس النص في أكثر من مكان بدون سبب.
- أي تغيير كبير يتقسم إلى PR صغير أو مرحلة مستقلة، مع `pnpm test` و `pnpm build`.

## المهمة 1: تبسيط التطبيق أكثر

**الكود:** `RAI-070`

**الهدف:** إزالة التكرار والتشتت قبل إضافة مزايا جديدة.

**النطاق:**

- مراجعة صفحات `/`, `/today`, `/chat`, `/calendar`, `/settings`.
- دمج أو إزالة أي صندوق أو زر أو نص يكرر نفس الوظيفة.
- تبسيط الإعدادات لتكون مجموعات قليلة وواضحة: الحساب، التقويم، التذكيرات، الخصوصية.
- توحيد مداخل إضافة المهمة: لا يكون عندنا أكثر من طريقة متشابهة تشوش المستخدم.
- مراجعة النصوص العربية وتقليل الشرح داخل الواجهة.

**ملفات متوقعة:**

- `src/app/page.tsx`
- `src/app/today/page.tsx`
- `src/app/settings/page.tsx`
- `src/components/shell/BottomNav.tsx`
- `src/components/shell/TopBar.tsx`
- `src/components/chat/MessageComposer.tsx`

**معايير القبول:**

- لا يوجد إجراء مكرر واضح بين الرئيسية واليوم والإعدادات.
- الإعدادات تعرض اختيارات فعلية فقط أو حالات تكامل واضحة.
- الصفحة الرئيسية لا تتنافس مع صفحة اليوم؛ تكون ملخصاً خفيفاً فقط.
- `pnpm test` و `pnpm build` ناجحان.

## المهمة 2: تغيير التصميم

**الكود:** `RAI-080`

**الهدف:** تصميم أخف وأكثر نضجاً، مناسب لتطبيق إنتاجية يومي وليس صفحة عرض.

**النطاق:**

- تثبيت نمط بصري جديد للشاشات الأساسية.
- تقليل الكروت المتداخلة، وتكثيف المعلومات بدون زحمة.
- تحسين التقويم ليشعر كأداة يومية قوية: يوم، أسبوع، شهر.
- تحسين قوائم المهام والإجراءات السريعة والسحب.
- توحيد المسافات، أحجام النصوص، الأزرار، والحالات الفارغة.

**ملفات متوقعة:**

- `src/app/globals.css`
- `tailwind.config.ts`
- `src/app/page.tsx`
- `src/app/today/page.tsx`
- `src/app/calendar/page.tsx`
- `src/app/chat/page.tsx`
- `src/components/ui/*`
- `src/components/plan/*`

**معايير القبول:**

- تجربة موبايل أولاً تبدو متماسكة في 360px و 390px و 430px.
- لا يوجد نص يتداخل أو يخرج من الأزرار أو البطاقات.
- التقويم والمهام قابلة للمسح البصري السريع.
- فحص متصفح محلي للصفحات الأساسية مع لقطات مراجعة.
- `pnpm test` و `pnpm build` ناجحان.

## المهمة 3: نظام التذكير والمتابعة والإحصائيات

**الكود:** `RAI-090`

**الهدف:** جعل رتّبها يساعد المستخدم بعد التخطيط، وليس وقت التخطيط فقط.

**النطاق الأول: نموذج البيانات**

- إضافة حقول متابعة لكل مهمة:
  - وقت البداية والنهاية المخطط.
  - وقت البدء الفعلي.
  - وقت الإنجاز الفعلي.
  - سبب التأجيل أو الفشل عند وجوده.
  - التصنيف: صحة، عمل، تعلم، علاقات، راحة، التزامات.
  - مستوى الطاقة والتركيز قبل وبعد المهمة إن احتجناه.
- إضافة نوع Reminder:
  - قبل المهمة.
  - عند بداية المهمة.
  - متابعة بعد انتهاء المهمة لو لم تغلق.

**النطاق الثاني: التذكيرات والمتابعة**

- شاشة أو bottom sheet لتحديد تذكير سريع لكل مهمة.
- جدولة محلية أولاً باستخدام Web Notifications عند دعم المتصفح.
- fallback داخل التطبيق لو الإشعارات غير مفعلة.
- متابعة ذكية بعد وقت المهمة: تم؟ تأجلت؟ تحتاج إعادة جدولة؟

**النطاق الثالث: الإحصائيات**

- إجمالي الوقت المخطط مقابل المنجز.
- توزيع الوقت حسب التصنيف.
- وقت الأشياء الحسنة: صحة، تعلم، راحة، علاقات.
- معدل الالتزام بالمواعيد.
- أكثر أوقات اليوم إنتاجية.
- المهام التي تتأجل كثيراً.
- متوسط مدة الانحراف عن الخطة.
- اقتراحات بسيطة من سراج بناءً على النمط.

**ملفات متوقعة:**

- `src/lib/types.ts`
- `src/lib/store/tasks-store.ts`
- `src/lib/reminders/*`
- `src/lib/analytics/*`
- `src/components/reminders/*`
- `src/components/analytics/*`
- `src/app/today/page.tsx`
- `src/app/calendar/page.tsx`
- `src/app/settings/page.tsx`

**معايير القبول:**

- يمكن إضافة تذكير لمهمة محددة.
- عند مرور وقت مهمة غير منجزة، يظهر follow-up واضح داخل التطبيق.
- توجد لوحة إحصائيات أولية مفيدة وغير مزدحمة.
- الحسابات الأساسية مغطاة باختبارات Vitest.
- النظام يعمل بدون Supabase، ثم يكون جاهزاً للمزامنة لاحقاً.

## تقسيم العمل المقترح

**المطور 1: التبسيط**

- يملك `RAI-070`.
- لا يغير نموذج البيانات.
- يسلّم واجهة أبسط ونظافة أعلى في المسارات الحالية.

**المطور 2: التصميم**

- يملك `RAI-080`.
- يعمل بعد أو بالتوازي مع التبسيط، لكن لا يبدأ تغييرات كبيرة في نفس الملفات بدون تنسيق.
- يسلّم لقطات للشاشات الأساسية.

**المطور 3: المتابعة والإحصائيات**

- يملك `RAI-090`.
- يبدأ من types و analytics helpers والاختبارات.
- لا يربط إشعارات حقيقية قبل تثبيت نموذج البيانات وحسابات الإحصائيات.

## ترتيب التنفيذ

1. `RAI-070` أولاً لأن إضافة مزايا فوق واجهة مزدحمة سيزيد التعقيد.
2. `RAI-080` ثانياً بعد أن تستقر خريطة الشاشات.
3. `RAI-090` ثالثاً، ويقسم إلى PRs: نموذج بيانات، analytics، reminders، UI.

## Definition of Done

- كل PR يذكر الملفات التي لمسها ولماذا.
- كل PR يحدث هذا الملف أو `IMPLEMENTATION_TASKS.md` عند تغيير نطاق المهمة.
- لا توجد تغييرات عشوائية في `design/` أو ملفات غير مرتبطة.
- `pnpm test` ناجح.
- `pnpm build` ناجح.
- فحص متصفح محلي لأي شاشة تم تعديلها.
